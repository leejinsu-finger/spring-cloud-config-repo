zuul:
  routes:
    user-service:
      serviceId: user-service  
      path: /api/user/**
    todo-service:
      serviceId: todo-service
      path: /api/todo/**

ribbon:
  eureka:
    enabled: false      

spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest      

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/    
